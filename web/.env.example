# 本機開發時通常「不用」設這兩個 —— vite.config.ts 已經設了 proxy，
# 留空就會走同源的 /api 與 /ws，由 proxy 轉發到 localhost:8000。
#
# 部署到 Vercel 時才需要設（Vercel 的 Environment Variables）：
# VITE_API_BASE=https://你的服務.onrender.com
# VITE_APP_TOKEN=跟後端 APP_SHARED_TOKEN 一樣的那組字串

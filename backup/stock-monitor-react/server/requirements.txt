# server/ 執行所需（Render 的 Build Command 用這份）
#   pip install -r server/requirements.txt
#
# 注意：這份刻意「不含」streamlit / plotly / streamlit-autorefresh，
# 那些只有舊版 Streamlit UI 需要。少裝這些對 Render 免費方案的
# 512MB 記憶體與冷啟動時間都有明顯幫助。
fastapi>=0.115
uvicorn[standard]>=0.30
pandas
yfinance
requests
urllib3
openpyxl

# 富邦 SDK：從 repo 內的 whl 安裝（manylinux x86_64，Render 的執行環境相容）
./fubon_neo-2.2.8-cp37-abi3-manylinux_2_17_x86_64.manylinux2014_x86_64.whl
